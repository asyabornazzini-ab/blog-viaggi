import { defineConfig } from 'astro/config';

// Configurazione di Astro.
// L'URL qui sotto e' provvisorio: lo cambieremo quando registreremo il dominio.
export default defineConfig({
  site: 'https://blog-viaggi.pages.dev',
});
